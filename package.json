{
  "name": "footbazed",
  "version": "0.4.0",
  "private": true,
  "description": "Football ratings, profiles and community platform",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "serve": "node scripts/serve.mjs",
    "test:e2e": "node scripts/run-e2e.mjs",
    "check": "node scripts/check-static.mjs && node --test tests/*.test.js",
    "check:all": "node scripts/check-static.mjs && node --test tests/*.test.js && node scripts/run-e2e.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
