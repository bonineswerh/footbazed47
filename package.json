{
  "name": "footbazed",
  "version": "0.4.0",
  "private": true,
  "description": "Football ratings, profiles and community platform",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-static.mjs && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
