{
  "name": "footbazed",
  "version": "0.4.0",
  "private": true,
  "description": "Football ratings, profiles and community platform",
  "packageManager": "pnpm@11.16.0",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:migrations": "node scripts/check-migrations.mjs",
    "serve": "node scripts/serve.mjs",
    "test:e2e": "node scripts/run-e2e.mjs",
    "typecheck": "tsc -p tsconfig.json",
    "check:environment": "node scripts/check-environment.mjs",
    "check": "node scripts/check-environment.mjs && node scripts/check-static.mjs && node scripts/check-migrations.mjs && tsc -p tsconfig.json && node --test tests/*.test.js",
    "check:all": "node scripts/check-environment.mjs && node scripts/check-static.mjs && node scripts/check-migrations.mjs && tsc -p tsconfig.json && node --test tests/*.test.js && node scripts/run-e2e.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@fontsource-variable/onest": "5.3.0",
    "@fontsource/bebas-neue": "5.3.0",
    "@supabase/supabase-js": "2.112.2"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "axe-core": "4.13.0",
    "typescript": "7.0.2"
  }
}
